#
# TI's shared transport line discipline and the protocol
# drivers (BT, FM and GPS)
#
menu "Synaptics fingerprint SPI driver"
config FPR_SYN
	bool "SPI driver for fingerprint"
    default n
	help
         SPI driver for finger print
endmenu
