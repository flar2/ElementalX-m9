#
# TI's shared transport line discipline and the protocol
# drivers (BT, FM and GPS)
#
menu "FingerprintCard fingerprint REE SPI driver"
config FPR_FPC_REE
       default n
       tristate "FPC_REE fingerprint sensor support"
       depends on SPI_MASTER
endmenu
